# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
build/
dist/

# Env / secrets
.env
.env.*
!.env.example

# Virtualenv
.venv/
venv/
env/

# IDE
.idea/
.vscode/
*.swp

# Test / coverage
.pytest_cache/
.coverage
htmlcov/

# OS
.DS_Store

# Docker volumes (compose 创建在容器内，本地若有挂载导出再忽略)
pgdata/
redisdata/

# 评测抽样导出（含采样正文，按需本地生成，不入库）
eval_classify_sample*.csv

# Celery beat 本地调度状态（运行时生成，不入库）
celerybeat-schedule.db
celerybeat-schedule

# iOS / Xcode（构建产物与用户态文件不入库；工程与源码入库）
ios/**/build/
ios/**/DerivedData/
ios/**/*.xcuserstate
ios/**/xcuserdata/
ios/**/.swiftpm/
ios/**/*.xcscmblueprint
*.ipa
*.dSYM.zip

# Alembic 自动生成的版本暂不忽略（需要纳入版本管理）
